# InspireHub Mobile iOS - SwiftUI Design Guide

> **Version**: 1.1.0
> **Last Updated**: 2026-02-02
> **対象**: Phase1 全8画面 + Phase2 マップ画面ビジュアルコンセプト案
> **ステータス**: マップ画面ビジュアルは要チーム検討（Section A-2）

---

## A. デザインコンセプト

### A-1. UX方針（ペルソナ・ジャーニーマップから導出）

#### ターゲットペルソナ

中村大輔（31歳）、Webバックエンドエンジニア。完全リモート。技術キャッチアップは好きだがアイデア出しは苦手。「最初の一歩を気軽に踏み出せる環境が欲しい」。

#### ペルソナから導く4つのUX原則

| 原則 | ペルソナの特徴 | UI設計への示唆 |
|------|-------------|---------------|
| **1. 軽い関与から始められる** | 自分から投稿するのは腰が重い。リアクションで"軽く関与"するところから入る | リアクションはワンタップ。投稿は段階的に誘導。FABは常時表示だが主張しすぎない |
| **2. 見て楽しい** | 他の人が出した課題やアイデアを見るのが好き。盛り上がりを感じると参加したくなる | マップ画面の視覚的魅力。カード一覧のスキャンしやすさ。リアクション数の可視化 |
| **3. 恥ずかしくない** | オープンな場にアイデアを投稿することへの恥ずかしさがある | 投稿フォームのテンプレ表示。「完璧でなくていい」雰囲気のUI。柔らかいトーン |
| **4. 仲間が見える** | 「気の合うチーム」で動くと力を発揮。Slack DMは重い | 「作ってみたい」リアクション。興味ユーザー一覧。温度感の可視化 |

#### 段階的エンゲージメント設計

```
閲覧（見て楽しい） → リアクション（ワンタップ） → コメント（軽い一言） → 投稿（創造）
         ↑                    ↑                      ↑                    ↑
    心理負荷: 0          心理負荷: 低           心理負荷: 中          心理負荷: 高
    → UIの魅力で引く     → 最小タップで完結    → テンプレ支援      → 例文・FABで後押し
```

#### ジャーニーマップに基づくタッチポイント設計

| タイミング | ユーザー行動 | 必要な体験 | UI対応 |
|-----------|-----------|----------|--------|
| 業務の合間 | PCで軽く開く → スマホでも確認 | すぐ内容が目に入る | ホーム一覧のスキャンしやすいカードデザイン |
| 気になるアイデア発見 | リアクションしたい | ワンタップで完結 | カード上のリアクションボタン（詳細に遷移不要） |
| 盛り上がりを感じた時 | 参加したくなる | 仲間が見える・温度感がわかる | リアクション数表示、マップでの活性度の視覚表現 |
| ふと思いついた時 | 軽いメモ感覚で投稿 | 構えずに書ける | FABからの投稿導線、テンプレ表示 |

### A-2. マップ画面ビジュアルコンセプト案【要チーム検討】

マップ画面はアプリの核となる画面。ノード関係の可視化方法について、以下4案を提示する。

#### 案1: シャボン玉（Bubble Float）

```
     ○     ○
    ╱ ╲   ╱
   ○   ○─○
    ╲
     ○
```

**コンセプト**: ノードが半透明のシャボン玉として浮遊。ふわふわ漂い、派生関係は柔らかい曲線で結ばれる。

| 項目 | 内容 |
|------|------|
| **雰囲気** | 柔らかい・親しみやすい・夢想的 |
| **ノード表現** | 半透明Circle + RadialGradient。サイズはリアクション数に比例（40〜80pt） |
| **接続線** | ベジェ曲線（QuadCurve）、`Color.secondary.opacity(0.3)` |
| **アニメーション** | 微小なY方向ゆらぎ（±3pt、2.5〜3.5秒周期）。各ノードの位相をずらす |
| **iOS技術** | **SwiftUI + Canvas** or **SpriteKit**（`SKScene` をSwiftUIでホスト） |
| **難易度** | ★★☆☆（SwiftUI Canvas: 中。SpriteKit: やや高） |
| **メリット** | ペルソナの「見て楽しい」に最も合致。浮遊感で探索意欲を刺激。柔らかいトーンで「恥ずかしさ」を軽減 |
| **デメリット** | ノード数が多い場合のパフォーマンス（100+ノードで要最適化）。ラベル表示スペースが限られる |

#### 案2: 星座（Constellation）

```
     ✦ ─── ✦
    ╱         ╲
   ✦           ✦
    ╲         ╱
     ✦ ─── ✦
```

**コンセプト**: ノードが星のように配置され、派生関係が星座の線で結ばれる。ダークモードで映える。

| 項目 | 内容 |
|------|------|
| **雰囲気** | 知的・探索的・宇宙的 |
| **ノード表現** | 光る点 + Glow Effect。ホットなノードは明るく輝く |
| **接続線** | 直線または軽い曲線。`Color.white.opacity(0.3)`（ダーク）/ `Color.primary.opacity(0.2)`（ライト） |
| **アニメーション** | 微小な輝度のパルス（0.8〜1.0 opacity、3秒周期）。新規ノード出現時にフラッシュ |
| **iOS技術** | **SwiftUI Canvas** + `blur` / `shadow` or **Metal**（大規模時） |
| **難易度** | ★★☆☆（基本実装は容易。Glow表現に工夫必要） |
| **メリット** | ダークモードで非常に美しい。「つながり」の可視化が直感的。知的好奇心を刺激 |
| **デメリット** | ライトモードでは印象が弱い。「柔らかさ」「親しみやすさ」は他案に劣る |

#### 案3: マインドマップ（Tree/Force-Directed Graph）

```
          ┌─ Node3
    ┌─ Node2 ─┤
Root ┤         └─ Node4
    └─ Node5
```

**コンセプト**: 古典的なノードグラフ。Force-Directed Layoutでノードが物理法則に従い自然配置される。

| 項目 | 内容 |
|------|------|
| **雰囲気** | 整理された・論理的・情報密度高 |
| **ノード表現** | 角丸Rectangle + テキストラベル。色でノードタイプを区別 |
| **接続線** | 直線 or 矢印付き線。派生の方向が明確 |
| **アニメーション** | Force Simulation による自然配置。ドラッグ時のバネ物理 |
| **iOS技術** | **SpriteKit**（物理シミュレーション内蔵）or **Core Animation** + 自前の力学計算 |
| **難易度** | ★★★☆（Force-Directed Layoutの実装がやや複雑） |
| **メリット** | 情報量が最も多い。派生関係の構造が一目瞭然。エンジニアペルソナに馴染みやすい |
| **デメリット** | 「楽しい」「浮遊感」は弱い。見た目が業務ツール的になりがち。初見のインパクト薄 |

#### 案4: 水面の波紋（Ripple/Pond）

```
       ~~○~~
     ~╱~~~~~╲~
    ○~~~~~~~~~○
     ~╲~~~~~╱~
       ~~○~~
```

**コンセプト**: ノードが水面に浮かぶ葉のような存在。タップすると波紋が広がり、派生ノードがリプルで現れる。

| 項目 | 内容 |
|------|------|
| **雰囲気** | 穏やか・自然・瞑想的 |
| **ノード表現** | 丸いカード（水面の葉/蓮）。影で浮遊感。背景に淡いグラデーション |
| **接続線** | 波紋の同心円で暗示（明示的な線なし）。または水面下の薄い線 |
| **アニメーション** | タップ時の波紋エフェクト（CircleのscaleEffect + opacity fade）。穏やかな揺らぎ |
| **iOS技術** | **SwiftUI** + `Canvas` + カスタムシェイプ or **Metal Shader**（波紋の品質重視時） |
| **難易度** | ★★★★（Metal使用時は高。SwiftUIのみなら★★★） |
| **メリット** | 独創的で記憶に残る。「穏やかさ」で投稿の心理的ハードルを下げる |
| **デメリット** | 派生関係の可視化が弱い。実装コストが高い。ノードが多い場合の視認性に課題 |

#### 比較マトリクス

| 評価軸 | シャボン玉 | 星座 | マインドマップ | 水面の波紋 |
|-------|----------|------|------------|----------|
| ペルソナ適合度 | ◎ | ○ | △ | ○ |
| 「見て楽しい」 | ◎ | ◎ | △ | ◎ |
| 情報の伝達力 | ○ | ○ | ◎ | △ |
| 実装難易度（低が易） | 低〜中 | 低〜中 | 中〜高 | 高 |
| パフォーマンス | ○ | ◎ | ○ | △ |
| ダークモード映え | ○ | ◎ | ○ | ○ |
| 独創性 | ○ | ○ | △ | ◎ |

> **推奨**: Phase1は現状のリスト表示を維持。Phase2でチーム検討の上、上記いずれかを選択。
> 技術的には案1（シャボン玉）または案2（星座）が実装コストと体験のバランスが良い。

---

## B. カラーパレット

### 基本方針

iOS標準のセマンティックカラーをベースに、ブランドカラーとアクセントで統一感を持たせる。

### ブランドカラー

| 名前 | 用途 | Light Mode | Dark Mode | SwiftUI |
|------|------|-----------|-----------|---------|
| **Primary** | ナビゲーション、FAB、リンク | `#007AFF` | `#0A84FF` | `Color.blue` |
| **Accent** | アイデア系アクション、ロゴ | `#FF9500` | `#FF9F0A` | `Color.orange` |
| **Warm Yellow** | アイデアバッジ、ひらめき表現 | `#FFCC00` | `#FFD60A` | `Color.yellow` |

### セマンティックカラー

| 名前 | 用途 | SwiftUI |
|------|------|---------|
| **Background** | 画面背景 | `Color(.systemBackground)` |
| **Card Background** | カード・セル背景 | `Color(.secondarySystemBackground)` |
| **Group Background** | グループ背景（Form等） | `Color(.systemGroupedBackground)` |
| **Primary Text** | 主要テキスト | `.primary` |
| **Secondary Text** | 補助テキスト、日時、説明 | `.secondary` |
| **Error** | エラー表示 | `Color.red` |

### ノードタイプカラー

| ノード種別 | アイコン色 | バッジ背景 | バッジテキスト |
|-----------|----------|-----------|-------------|
| **課題（Issue）** | `Color.orange` / `Color.red`（詳細画面） | `Color.orange.opacity(0.15)` | `Color.orange` |
| **アイデア（Idea）** | `Color.yellow` / `Color.orange`（詳細画面） | `Color.yellow.opacity(0.15)` | `Color.yellow` |

> **要統一**: 課題のアイコン色がHomeView（orange）とDetailView（red）で異なる。統一推奨。

### リアクションカラー

| リアクション | 表示 | 色 |
|------------|------|-----|
| いいね | 👍 | `.blue`（アクティブ時） |
| 共感 | 💡 | `.orange`（アクティブ時） |
| 気になる | 👀 | `.purple`（アクティブ時） |
| 作りたい | 🤝 | `.green`（アクティブ時） |

---

## C. タイポグラフィ

### 基本方針

iOS標準のSan Francisco（SF Pro）フォントファミリーを使用。Dynamic Typeに完全対応。

### テキストスタイル一覧

| 用途 | SwiftUI Font | Weight | 使用箇所 |
|------|-------------|--------|---------|
| アプリタイトル | `.largeTitle` | `.bold` | LoginView ロゴ |
| 画面タイトル | `.title2` | `.bold` | 詳細画面ノードタイトル |
| セクション見出し | `.headline` | `.regular` | 各セクションタイトル |
| カードタイトル | `.headline` | `.regular` | NodeCardView タイトル |
| 本文 | `.body` | `.regular` / `.medium` | 詳細画面本文、リスト行タイトル |
| 補助テキスト | `.subheadline` | `.regular` | 説明文、エラーメッセージ |
| キャプション | `.caption` | `.regular` | 日時、投稿者名、タグ |
| 小キャプション | `.caption2` | `.regular` | NodeCardView バッジ、日時 |

### 行間・テキスト制限

| コンテキスト | lineSpacing | lineLimit |
|------------|-------------|-----------|
| 詳細画面本文 | `4` | なし |
| カードタイトル | デフォルト | `2` |
| カード本文プレビュー | デフォルト | `3` |
| リスト行タイトル | デフォルト | `2` |
| リスト行サブテキスト | デフォルト | `1` |
| 派生元引用本文 | デフォルト | `2` |

---

## D. 画面別レイアウト仕様

### D-1. ログイン画面（LoginView）

```
┌─────────────────────────────┐
│         Spacer()            │
│    💡 (80pt, orange)        │  ← lightbulb.fill
│    "InspireHub"             │  ← .largeTitle.bold
│    "社内ハッカソンを..."      │  ← .subheadline, .secondary
│         Spacer()            │
│  ┌───────────────────────┐  │
│  │  G  Googleでログイン    │  │  ← padding: 32H, cornerRadius: 12
│  └───────────────────────┘  │
│         Spacer()            │
└─────────────────────────────┘
```

| 要素 | spacing |
|------|---------|
| VStack全体 | `32` |
| ロゴ内VStack | `16` |
| ボタン padding(.horizontal) | `32` |

### D-2. メインタブ画面（MainTabView）

```
┌─────────────────────────────┐
│     [各タブのコンテンツ]       │
│                       ┌──┐  │
│                       │＋│  │  ← FAB: 56x56, blue, Circle
│                       └──┘  │     trailing: 20, bottom: 80
├─────────────────────────────┤
│  🏠 ホーム  🗺 マップ  👤 マイ │
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| FAB サイズ | 56 x 56 pt |
| FAB アイコン | `"plus"`, `.title2.bold`, white |
| FAB 背景 | `Color.blue`, `Circle()` |
| FAB シャドウ | `black.opacity(0.2)`, radius: 4, y: 2 |
| FAB 位置 | `.trailing: 20`, `.bottom: 80` |
| Tab アイコン | `house.fill` / `map.fill` / `person.fill` |

### D-3. ホーム画面（HomeView）

```
┌─────────────────────────────┐
│  InspireHub          ⇅      │  ← inline title + sort menu
├─────────────────────────────┤
│ [新着|課題|アイデア|自分]      │  ← 横スクロールタブ, blue underline
├─────────────────────────────┤
│ ┌─────────────────────────┐ │
│ │ 💡 課題         2時間前   │ │  ← NodeCardView
│ │ タイトル                  │ │
│ │ 本文プレビュー...          │ │
│ └─────────────────────────┘ │
│              spacing:12      │
│ ┌─────────────────────────┐ │
│ │ ...                     │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| タブバー padding(.top) | `4` |
| タブ項目 minWidth | `70` |
| タブ項目 padding(.horizontal) | `8` |
| アンダーライン | Rectangle, height: `2`, `Color.blue` |
| カード間 spacing | `12` |
| カードリスト padding(.horizontal) | `16` |
| カードリスト padding(.top) | `8` |
| カード内 padding | `14` |
| カード cornerRadius | `12` |
| カード背景 | `Color(.secondarySystemBackground)` |

### D-4. マップ画面（MapView）

#### Phase1: リスト表示（現状実装済み）

```
┌─────────────────────────────┐
│           マップ              │
├─────────────────────────────┤
│ 🔴 課題タイトル           >   │  ← ルートノード
│ ─────────────────────────── │
│     💡 派生アイデア        >   │  ← インデント24pt
│ ─────────────────────────── │
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| 行 padding(.horizontal) | `16` |
| 行 padding(.vertical) | `12` |
| アイコンとテキスト spacing | `12` |
| 子ノードインデント | `24` |
| Divider leading padding | ルート: `16`, 子: `52` |

#### Phase2: ビジュアルマップ（Section A-2 の案から選択）

選択された案に応じて仕様を策定する。共通仕様:

| 要素 | 仕様 |
|------|------|
| ジェスチャー | ピンチ: 拡大縮小 / ドラッグ: パン / タップ: 詳細遷移 |
| ノードサイズ | リアクション数に応じて 40〜80pt |
| ノードラベル | `.caption2`, 中央配置 |
| タップ反応 | `scaleEffect(1.1)` + spring |
| ツールバー | [リセット] [フィルタ] (Phase2) |

### D-5. マイページ画面（MyPageView）

```
┌─────────────────────────────┐
│        マイページ              │
├─────────────────────────────┤
│      👤 (72pt, blue)        │
│      ユーザー名               │  ← .title2.bold
│      [Backend Engineer]      │  ← TagChip style
│  自分の投稿  3                │  ← .headline + count
│ ─────────────────────────── │
│  🔴 課題タイトル           >  │
│ ─────────────────────────── │
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| プロフィール padding(.top) | `16` |
| プロフィール内 spacing | `12` |
| アイコンサイズ | `72pt` |
| roleTag padding | H: `12`, V: `4` |
| roleTag 背景 | `Color.blue.opacity(0.1)` |
| セクション全体 spacing | `24` |
| セクション見出し padding(.horizontal) | `16` |

### D-6. 詳細画面（DetailView）

```
┌─────────────────────────────┐
│  ←                          │
├─────────────────────────────┤
│ 🔴 課題                     │  ← badge + .title2.bold
│ タイトル                     │
│ 本文本文...                  │  ← .body, lineSpacing: 4
│ ─────────────────────────── │
│ 👤 投稿者名                  │
│ ─────────────────────────── │
│ ← 派生元ノードを見る          │  ← (ある場合のみ)
│ 👍  💡  👀  🤝              │  ← リアクションバー
│ [派生アイデアを投稿]          │  ← orange, cornerRadius: 10
│ 派生ノード                    │
│  └─ 子ノードタイトル       >  │
│ コメント                     │
│ [入力欄...           ✈]     │
│ ┌─────────────────────┐     │
│ │ 👤 ユーザーA: ...     │     │
│ └─────────────────────┘     │
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| 全体 padding | `16` |
| セクション間 spacing | `20` |
| ヘッダー内 spacing | `8` |
| type badge padding | H: `8`, V: `2`, cornerRadius: `4` |
| リアクションバー spacing | `16`, V-padding: `4` |
| 派生ボタン padding | `12`, cornerRadius: `10` |
| 親ノードリンク padding | `12`, cornerRadius: `8` |
| コメント行 padding | `10`, cornerRadius: `8` |

### D-7. 投稿画面（IssuePostView / IdeaPostView）

```
┌─────────────────────────────┐
│ キャンセル   課題を投稿   投稿  │
├─────────────────────────────┤
│ タイトル                     │
│ [入力フィールド]              │  ← Form > Section
│ 本文                        │
│ [TextEditor, minH: 150]     │
│ タグ                        │
│ [タグ入力           ⊕]      │
│ [tag1] [tag2]               │  ← FlowLayout + TagChip
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| Form スタイル | iOS標準 Grouped |
| TextEditor minHeight | `150` |
| TagChip padding | H: `10`, V: `5` |
| TagChip 背景 | `Color.blue.opacity(0.1)` |
| TagChip cornerRadius | `8` |
| FlowLayout minimum | `80`, spacing: `8` |
| 投稿中オーバーレイ | ProgressView + bg opacity 0.9, cornerRadius: 12 |

### D-8. 派生投稿画面（DerivedPostView）

投稿画面と同構造。上部に派生元ノードの引用セクション追加。

| 要素 | 仕様 |
|------|------|
| 派生元行 padding(.vertical) | `4` |
| 本文 lineLimit | `2` |

### D-9. 投稿種別選択（PostTypeSelectSheet）

```
┌─────────────────────────────┐
│ 閉じる                       │
│    投稿タイプを選択            │  ← .title2.bold, top: 24
│ ┌─────────────────────────┐ │
│ │ ⚠ 課題を投稿           > │ │  ← systemGray6, cornerRadius: 12
│ │   解決したい問題...       │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │ 💡 アイデアを投稿       > │ │
│ └─────────────────────────┘ │
└─────────────────────────────┘
```

| 要素 | 仕様 |
|------|------|
| VStack spacing | `20` |
| ボタン padding(.horizontal) | `16` |
| ボタン背景 | `Color(.systemGray6)` |
| ボタン cornerRadius | `12` |
| presentationDetents | `.medium` |

---

## E. 共通コンポーネント

### E-1. NodeCardView（ノードカード）

ホーム画面で使用。タップで詳細に遷移。

```swift
VStack(alignment: .leading, spacing: 8) {
    HStack { nodeTypeBadge; Spacer(); dateText }
    titleText   // .headline, lineLimit: 2
    contentText // .subheadline, .secondary, lineLimit: 3
}
.padding(14)
.background(Color(.secondarySystemBackground))
.cornerRadius(12)
```

**バッジ仕様**: HStack spacing `4`, Icon `.caption2`, Text `.caption2.medium`, Padding H `8` V `3`, cornerRadius `6`.

### E-2. TagChip（タグチップ）

```swift
Text(text)
    .font(.caption)
    .padding(.horizontal, 10).padding(.vertical, 5)
    .background(Color.blue.opacity(0.1))
    .foregroundColor(.blue)
    .cornerRadius(8)
```

### E-3. ボタンスタイル

| スタイル | 用途 | 仕様 |
|---------|------|------|
| **Primary** | ログイン、投稿 | maxWidth: infinity, blue bg, white text, cornerRadius: 12 |
| **Action** | 派生投稿 | maxWidth: infinity, orange bg, white text, cornerRadius: 10 |
| **FAB** | 新規投稿 | 56x56, blue, Circle, shadow |
| **Bordered** | 再読み込み | `.buttonStyle(.bordered)` |
| **Plain** | カードタップ、リスト行 | `.buttonStyle(.plain)` |

### E-4. ナビゲーションパターン

| パターン | 使用箇所 | 実装 |
|---------|---------|------|
| **TabView** | メイン3画面 | `TabView(selection:)` + `.tabItem` |
| **NavigationView** | 各タブ内、モーダル内 | `NavigationView { ... }` |
| **NavigationLink** | リスト→詳細 | `NavigationLink(destination:)` |
| **Sheet** | 投稿画面 | `.sheet(isPresented:)` |
| **BottomSheet** | 投稿タイプ選択 | `.presentationDetents([.medium])` |

### E-5. 状態表示パターン

全画面で統一する3状態:

```swift
// Loading（データ未取得時）
if viewModel.isLoading && data.isEmpty { ProgressView("読み込み中...") }

// Error（データ未取得時）
if let error = viewModel.error, data.isEmpty {
    VStack(spacing: 16) {
        Image(systemName: "exclamationmark.triangle").font(.system(size: 40)).foregroundColor(.orange)
        Text(error).font(.subheadline).foregroundColor(.secondary)
        Button("再読み込み") { viewModel.load() }.buttonStyle(.bordered)
    }
}

// Empty
VStack(spacing: 16) {
    Image(systemName: contextIcon).font(.system(size: 40)).foregroundColor(.secondary.opacity(0.5))
    Text("データがありません").font(.subheadline).foregroundColor(.secondary)
}
```

### E-6. アイコン体系（SF Symbols）

| 用途 | アイコン名 |
|------|----------|
| ホームタブ | `house.fill` |
| マップタブ | `map.fill` |
| マイページタブ | `person.fill` |
| FAB | `plus` |
| 課題 | `exclamationmark.triangle.fill` / `exclamationmark.circle.fill` |
| アイデア | `lightbulb.fill` |
| ユーザー | `person.circle` / `person.circle.fill` |
| ソート | `arrow.up.arrow.down` |
| 派生元 | `arrow.turn.up.left` |
| 派生先 | `arrow.turn.down.right` |
| 次へ | `chevron.right` |
| 派生投稿 | `plus.bubble` |
| コメント送信 | `paperplane.fill` |
| タグ追加 | `plus.circle.fill` |
| エラー | `exclamationmark.triangle` |
| アプリロゴ | `lightbulb.fill` |

---

## F. アニメーション指針

### F-1. 画面遷移（iOS標準準拠）

| 遷移 | アニメーション |
|------|------------|
| タブ切替 | TabView デフォルト（即座切替） |
| Push遷移 | NavigationLink デフォルト（右からスライド） |
| Modal表示 | `.sheet` デフォルト（下からスライドアップ） |
| BottomSheet | `.presentationDetents` デフォルト |

### F-2. マイクロインタラクション

| アクション | アニメーション | SwiftUI |
|-----------|------------|---------|
| リアクション付与 | アイコンバウンス + カウント増加 | `.spring(response: 0.3, dampingFraction: 0.5)` |
| タブ切替 | アンダーライン移動 | `.easeInOut(duration: 0.2)` |
| Pull to refresh | iOS標準 | `.refreshable` |
| 投稿完了 | Haptic + トースト | `UINotificationFeedbackGenerator` |
| カードタップ | 軽い押し込み | `scaleEffect(0.98)` on press |

### F-3. Haptic Feedback

| アクション | スタイル |
|-----------|---------|
| リアクション付与/解除 | `UIImpactFeedbackGenerator(style: .light)` |
| 投稿完了 | `UINotificationFeedbackGenerator().notification(.success)` |
| 投稿エラー | `UINotificationFeedbackGenerator().notification(.error)` |
| FABタップ | `UIImpactFeedbackGenerator(style: .medium)` |

### F-4. 投稿完了演出（Phase2）

投稿完了時の「ワクワク演出」。ペルソナの「投稿した！」という達成感を強化。
具体案はマップビジュアルとあわせてチーム検討。

---

## G. spacing・余白の統一基準

| 基準値 | 用途 |
|-------|------|
| `4` | 最小間隔（テキスト行間、微調整） |
| `8` | 小間隔（アイコンとテキスト、バッジ内） |
| `12` | 中間隔（カード間、セクション内要素間） |
| `16` | 標準余白（画面端パディング、セクション見出し） |
| `20` | セクション間（詳細画面の大区切り） |
| `24` | 大セクション間（マイページ内） |
| `32` | 画面レベルの大余白（ログイン画面ボタン周辺） |

---

## H. アクセシビリティ

| 項目 | 対応 |
|------|------|
| Dynamic Type | SwiftUI Fontスタイル使用で自動対応 |
| VoiceOver | カード・ボタンに `.accessibilityLabel` 設定 |
| カラーコントラスト | WCAG AA準拠（セマンティックカラー使用） |
| タッチターゲット | 最小 44pt |
| Reduce Motion | マップ浮遊アニメーションを `@Environment(\.accessibilityReduceMotion)` で無効化 |
